name: Test Frontend

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [main, master, develop]
    paths:
      - 'backend/app/**'
      - 'frontend/**'
      - '.github/workflows/test.yml'
  pull_request:
    branches: [main, master, develop]
    paths:
      - 'backend/app/**'
      - 'frontend/**'
      - '.github/workflows/test.yml'

jobs:
  test-frontend:
    runs-on: [self-hosted, windows]
    defaults:
      run:
        shell: pwsh

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install frontend dependencies
        run: npm ci
        working-directory: frontend

      # Phase D.5.2 — generated TS types are .gitignore'd; ts-rs only
      # produces them when ``cargo test`` runs. Without this step, a PR
      # that renames or adds a Rust IPC type would still pass ``npm run
      # test`` (vitest only treats type imports as elidable), and the
      # drift would only surface during the much-later Build job.
      #
      # NOTE: ``tauri::generate_context!()`` (which the lib pulls in via
      # ``tauri::Builder::default().run(generate_context!())``) eagerly
      # validates the configured ``frontendDist`` (``../dist``) at compile
      # time. We haven't actually built the frontend yet, so create an
      # empty placeholder directory just to satisfy the macro — the
      # contents don't matter for ``cargo test --lib``. Same trick the
      # Build workflow uses.
      - name: Regenerate TypeScript types from Rust (ts-rs)
        run: |
          New-Item -ItemType Directory -Force -Path "../dist" | Out-Null
          cargo test --lib --quiet
        working-directory: frontend/src-tauri

      - name: Run frontend tests
        run: npm run test
        working-directory: frontend

      # ``npm run build`` runs ``vue-tsc`` first, which is the strictest
      # type check we have. Putting it after ts-rs regeneration means a
      # mismatched Rust ↔ TS schema will fail here in the CI rather than
      # in the Build workflow that runs much later.
      - name: Type-check frontend (vue-tsc + vite build)
        run: npm run build
        working-directory: frontend

      # Phase D.5.1 — TaskErrorCode 三层一致性 already runs as a
      # pre-commit hook; surface the same check in CI so PR authors
      # who skip hooks (--no-verify) can't sneak drift in.
      - name: TaskErrorCode three-layer consistency check
        run: python scripts/check_error_code_drift.py

  # Phase A — CLI 端到端 smoke 测试。覆盖 "python -m app check → info →
  # process → resume_conflict" 完整数据流,验证 config 解析 → FFmpeg 调用
  # → NDJSON 输出的协议完整性。纯 CLI 驱动,无需 GUI。
  e2e-smoke:
    needs: test-frontend
    runs-on: [self-hosted, windows]
    defaults:
      run:
        shell: pwsh
    steps:
      - uses: actions/checkout@v6

      - name: Setup CI runtime environment
        run: ./scripts/setup-ci-runtime-env.ps1

      - name: Generate synthetic test video
        run: |
          ffmpeg -f lavfi -i testsrc=duration=1:size=1280x720:rate=30 `
                 -f lavfi -i sine=frequency=1000:duration=1 `
                 -pix_fmt yuv420p -c:v libx264 -c:a aac `
                 "$env:TEMP/vp-e2e-test.mp4" -y

      - name: Run CLI e2e smoke tests
        run: python -m pytest backend/tests/test_integration/test_cli_process_e2e.py -v
        env:
          VP_E2E_INPUT: ${{ env.TEMP }}\vp-e2e-test.mp4
          VP_E2E_OUTPUT_DIR: ${{ env.TEMP }}\vp-e2e-output

  # Phase A — UI 端到端测试。Playwright + WebView2 CDP 驱动真实 Tauri
  # 应用,覆盖 Vue 前端 → Tauri command → Python CLI → NDJSON 输出 → 前端
  # UI 更新的完整链路。
  e2e-ui:
    needs: test-frontend
    runs-on: [self-hosted, windows]
    defaults:
      run:
        shell: pwsh
    steps:
      - uses: actions/checkout@v6

      - name: Install frontend dependencies
        run: npm ci
        working-directory: frontend

      - name: Regenerate TypeScript types from Rust (ts-rs)
        run: |
          New-Item -ItemType Directory -Force -Path "../dist" | Out-Null
          cargo test --lib --quiet
        working-directory: frontend/src-tauri

      - name: Install Playwright browsers
        run: npx playwright install chromium
        working-directory: frontend

      - name: Generate synthetic test video
        run: |
          ffmpeg -f lavfi -i testsrc=duration=1:size=1280x720:rate=30 `
                 -f lavfi -i sine=frequency=1000:duration=1 `
                 -pix_fmt yuv420p -c:v libx264 -c:a aac `
                 "$env:TEMP/vp-e2e-test.mp4" -y

      - name: Build Tauri app (release)
        run: npm run tauri build -- --no-bundle
        working-directory: frontend
        env:
          CI: true

      - name: Run Playwright e2e tests
        run: npx playwright test e2e/ --reporter=list
        working-directory: frontend
        env:
          VP_E2E_INPUT: ${{ env.TEMP }}\vp-e2e-test.mp4
          VP_E2E_OUTPUT_DIR: ${{ env.TEMP }}\vp-e2e-output
          VP_BACKEND_DIR: ${{ github.workspace }}\backend
          VP_FFMPEG_PATH: D:\ffmpeg-2025-08-11-git-3542260376-full_build\bin\ffmpeg.exe
          VP_FFPROBE_PATH: D:\ffmpeg-2025-08-11-git-3542260376-full_build\bin\ffprobe.exe
          VP_RIFE_MODEL_DIR: D:\tmp\vp-e2e-models
